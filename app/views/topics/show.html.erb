<p id="notice"><%= notice %></p>

<p>
  
  <%= @topic.name %>
</p>
<% id = @topic.id %>
<% @topic.articles.order(:sub_topic).each do |article|%>
<div id="<%article.sub_topic%>">
  <a href="<%=article.link%>"><%=article.description%></a><br>
</div>
<%end%>
<%= link_to 'Edit', edit_topic_path(@topic) %> |
<%= link_to 'Back', topics_path %>
